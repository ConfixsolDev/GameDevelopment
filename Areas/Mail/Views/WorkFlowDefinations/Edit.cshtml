@model TechWebSol.Areas.Mail.Models.WorkFlowDefination
@{
    ViewData["Title"] = "Workflow Defination";
}
<!-- Small Model for all -->
<div class="modal fade show" id="ModelForAll" tabindex="-1" role="dialog" aria-labelledby="clientModalLabel" aria-modal="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div id="ModelContentDiv"></div>
        </div>
    </div>
</div>
<div class="modal-header">
    <h4 class="modal-title" id="agencyModalLabel">Edit</h4>
    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
</div>
<div class="card card-shadow">
    <div class="card-body">
        <form asp-area="Mail" asp-action="Edit" asp-controller="WorkFlowDefinations" class="frmSubmit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="mb-3">
                <label asp-for="Name" class="control-label"></label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="mb-3">
                <label asp-for="IsActive" class="control-label"></label>
                <select asp-for="IsActive" class="form-select">
                    <option value="true">Yes</option>
                    <option value="false">No</option>
                </select>
                <span asp-validation-for="IsActive" class="text-danger"></span>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                <button type="submit" class="btn btn-primary">Save</button>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
@* <div class="container-fluid">
    <div class="card card-shadow">
        <div class="card-body">
            <hr class="mt-2 mb-4">
            <form asp-controller="WorkFlowDefinations" asp-action="Edit" class="row g-3">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <div class="col-md-4">
                    <div class="form-group">
                        <label asp-for="Name" class="control-label"></label>
                        <input asp-for="Name" class="form-control" />
                        <span asp-validation-for="Name" class="text-danger"></span>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="form-group">
                        <label asp-for="IsActive" class="control-label">Is Active</label>
                        <select asp-for="IsActive" class="form-select">
                            <option value="true">Yes</option>
                            <option value="false">No</option>
                        </select>
                    </div>
                </div>
                <div class="card-footer">
                    <button type="submit" class="btn btn-primary">Save</button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Breadcrump {
    <li class="breadcrumb-item"><a asp-action="Index" asp-controller="Home">Home</a></li>
    <li class="breadcrumb-item"><a asp-action="Index" asp-controller="WorkFlowDefinations">List</a></li>
    <li class="breadcrumb-item active">Edit</li>
}

@section Styles {

}

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
} *@

