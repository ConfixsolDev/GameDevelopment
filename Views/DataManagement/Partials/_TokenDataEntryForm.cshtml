@using static TechWebSol.Controllers.DataManagementController
@model TokenDataEntryViewModel

<!-- Token Data Entry Form Modal - Simple and Clean - Bootstrap Modal -->
<div class="modal fade" id="tokenDataEntryModal" tabindex="-1" role="dialog" aria-labelledby="tokenDataEntryModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-xl" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="tokenDataEntryModalLabel">
                    <i class="fas fa-database"></i> Data Entry - @Model.Token.Name
                </h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
            <div class="data-entry-container">
                <!-- Token Base Information (Primary) - Professional Dark Theme -->
         @*        <div class="token-base-section">
                    <div class="section-title-bar">
                        <i class="fas fa-crosshairs"></i> TOKEN BASE INFORMATION
                    </div>
                    <div class="token-content-row">
                    <div class="token-display">
                            <div class="token-preview-placeholder">
                                <i class="fas fa-crosshairs"></i>
                            </div>
                        <div class="token-details">
                            <h4>@Model.Token.Name</h4>
                                <p class="token-group-name">@Model.Token.TokenGroup?.Name</p>
                            <div class="token-status">
                                    <span class="badge badge-success-solid">ACTIVE TOKEN</span>
                                    <span class="badge badge-blue-solid">@Model.Token.ForceType.ToUpper()</span>
                            </div>
                        </div>
                    </div>
                        <div class="token-status-note">
                        <i class="fas fa-check-circle"></i> 
                            <span><strong>Token Selected:</strong> This token serves as the base for all brigade data management.</span>
                        </div>
                    </div>
                </div> *@

                <!-- Data Entry Form -->
                <div class="data-entry-form-section">
                    @if (Model.ExistingBrigade?.BrigadeCode != null)
                    {
                        <!-- Professional Brigade Header -->
                        <div class="brigade-section-professional">
                            <div class="section-title-bar">
                                <i class="fas fa-flag"></i> BRIGADE @Model.ExistingBrigade.BrigadeCode INFORMATION
                            </div>
                            <div class="brigade-info-row">
                                <div class="brigade-basic-info">
                                    <span class="brigade-label">Brigade:</span>
                                    <span class="brigade-code-display">Brigade @Model.ExistingBrigade.BrigadeCode</span>
                                    <span class="badge badge-blue-solid">@Model.ExistingBrigade.ForceType.ToUpper()</span>
                                </div>
                                <button type="button" class="btn btn-sm btn-outline-warning" onclick="editBrigadeCode()">
                                    <i class="fas fa-edit"></i> Edit Code
                                </button>
                            </div>
                        </div>
                        
                        <div class="brigade-data-form">
                            <!-- Compact Data Entry Tabs -->
                            <div class="data-entry-tabs compact-style">
                                <button type="button" class="data-tab active" onclick="showDataTab('infantry')">
                                    <i class="fas fa-user-shield"></i> Infantry Battalion
                                </button>
                                <button type="button" class="data-tab" onclick="showDataTab('armoured')">
                                    <i class="fas fa-shield-alt"></i> Armoured Regiment
                                </button>
                                <button type="button" class="data-tab" onclick="showDataTab('artillery')">
                                    <i class="fas fa-crosshairs"></i> Artillery Regiment
                                </button>
                                <button type="button" class="data-tab" onclick="showDataTab('logistics')">
                                    <i class="fas fa-truck"></i> Logistics Unit
                                </button>
                                <button type="button" class="data-tab" onclick="showDataTab('engineering')">
                                    <i class="fas fa-hard-hat"></i> Combat Engineering
                                </button>
                                <button type="button" class="data-tab" onclick="showDataTab('recon')">
                                    <i class="fas fa-binoculars"></i> Reconnaissance
                                </button>
                            </div>
                            
                            <div id="infantry-data" class="data-tab-content active">
                                <div class="compact-tab-header">
                                    <h6><i class="fas fa-user-shield text-primary"></i> Infantry Battalions</h6>
                                    <span class="unit-count-badge">
                                        @if (Model.ExistingInfantry != null && Model.ExistingInfantry.Any())
                                        {
                                            <span class="badge badge-primary">@Model.ExistingInfantry.Count() UNITS</span>
                                        }
                                    </span>
                                </div>
                                
                                <!-- Existing Infantry Battalions List -->
                                <div id="infantryBattalionsList" class="units-list">
                                    @if (Model.ExistingInfantry != null && Model.ExistingInfantry.Any())
                                    {
                                        @foreach (var battalion in Model.ExistingInfantry)
                                        {
                                            var battalionData = (dynamic)battalion;
                                            <div class="unit-item">
                                                <div class="unit-info">
                                                    <div class="unit-name1">@battalionData.Name</div>
                                                    <div class="unit-details">Code: @battalionData.UnitCode | Strength: @battalionData.Strength | Companies: @battalionData.Companies</div>
                                                </div>
                                                <div class="unit-actions">
                                                    <button class="btn btn-sm btn-outline-secondary" onclick="editBrigadeUnit('infantry', '@Model.Token.Id', '@Model.ExistingBrigade.Id', '@battalionData.Id')">
                                                        <i class="fas fa-edit"></i> Edit
                                                    </button>
                                                    <button class="btn btn-sm btn-outline-danger" onclick="deleteBrigadeUnit('infantry', '@battalionData.Id', '@Model.Token.Id', '@Model.ExistingBrigade.Id')">
                                                        <i class="fas fa-trash"></i> Delete
                                                    </button>
                                                </div>
                                            </div>
                                        }
                                    }
                                    else
                                    {
                                        <div class="no-units-message">No infantry battalions found. Click "Add New" to create one.</div>
                                    }
                                </div>
                                
                                <!-- Add New Button -->
                                <div class="add-unit-section">
                                    <button type="button" class="btn btn-primary" onclick="openInfantryBattalionForm('@Model.Token.Id', '@Model.ExistingBrigade.Id')">
                                        <i class="fas fa-plus"></i> Add New Infantry Battalion
                                    </button>
                                </div>
                            </div>
                            <div id="armoured-data" class="data-tab-content">
                                <div class="compact-tab-header">
                                    <h6><i class="fas fa-shield-alt text-warning"></i> Armoured Regiments</h6>
                                    <span class="unit-count-badge">
                                        @if (Model.ExistingArmoured != null && Model.ExistingArmoured.Any())
                                        {
                                            <span class="badge badge-warning">@Model.ExistingArmoured.Count() UNITS</span>
                                        }
                                    </span>
                                </div>
                                
                                <div id="armouredRegimentsList" class="units-list">
                                    @if (Model.ExistingArmoured != null && Model.ExistingArmoured.Any())
                                    {
                                        @foreach (var regiment in Model.ExistingArmoured)
                                        {
                                            var regimentData = (dynamic)regiment;
                                            <div class="unit-item">
                                                <div class="unit-info">
                                                    <div class="unit-name1">@regimentData.Name</div>
                                                    <div class="unit-details">Code: @regimentData.UnitCode | Strength: @regimentData.Strength | Tanks: @regimentData.Tanks</div>
                                                </div>
                                                <div class="unit-actions">
                                                    <button class="btn btn-sm btn-outline-secondary" onclick="editBrigadeUnit('armoured', '@Model.Token.Id', '@Model.ExistingBrigade.Id', '@regimentData.Id')">
                                                        <i class="fas fa-edit"></i> Edit
                                                    </button>
                                                    <button class="btn btn-sm btn-outline-danger" onclick="deleteBrigadeUnit('armoured', '@regimentData.Id', '@Model.Token.Id', '@Model.ExistingBrigade.Id')">
                                                        <i class="fas fa-trash"></i> Delete
                                                    </button>
                                                </div>
                                            </div>
                                        }
                                    }
                                    else
                                    {
                                        <div class="no-units-message">No armoured regiments found. Click "Add New" to create one.</div>
                                    }
                                </div>
                                
                                <div class="add-unit-section">
                                    <button type="button" class="btn btn-primary" onclick="openArmouredRegimentForm('@Model.Token.Id', '@Model.ExistingBrigade.Id')">
                                        <i class="fas fa-plus"></i> Add New Armoured Regiment
                                    </button>
                                </div>
                            </div>
                            <div id="artillery-data" class="data-tab-content">
                                <div class="compact-tab-header">
                                    <h6><i class="fas fa-crosshairs text-danger"></i> Artillery Regiments</h6>
                                    <span class="unit-count-badge">
                                        @if (Model.ExistingArtillery != null && Model.ExistingArtillery.Any())
                                        {
                                            <span class="badge badge-danger">@Model.ExistingArtillery.Count() UNITS</span>
                                        }
                                    </span>
                                </div>
                                
                                <div id="artilleryRegimentsList" class="units-list">
                                    @if (Model.ExistingArtillery != null && Model.ExistingArtillery.Any())
                                    {
                                        @foreach (var regiment in Model.ExistingArtillery)
                                        {
                                            var regimentData = (dynamic)regiment;
                                            <div class="unit-item">
                                                <div class="unit-info">
                                                    <div class="unit-name1">@regimentData.Name</div>
                                                    <div class="unit-details">Code: @regimentData.UnitCode | Strength: @regimentData.Strength | Guns: @regimentData.Guns</div>
                                                </div>
                                                <div class="unit-actions">
                                                    <button class="btn btn-sm btn-outline-secondary" onclick="editBrigadeUnit('artillery', '@Model.Token.Id', '@Model.ExistingBrigade.Id', '@regimentData.Id')">
                                                        <i class="fas fa-edit"></i> Edit
                                                    </button>
                                                    <button class="btn btn-sm btn-outline-danger" onclick="deleteBrigadeUnit('artillery', '@regimentData.Id', '@Model.Token.Id', '@Model.ExistingBrigade.Id')">
                                                        <i class="fas fa-trash"></i> Delete
                                                    </button>
                                                </div>
                                            </div>
                                        }
                                    }
                                    else
                                    {
                                        <div class="no-units-message">No artillery regiments found. Click "Add New" to create one.</div>
                                    }
                                </div>
                                
                                <div class="add-unit-section">
                                    <button type="button" class="btn btn-primary" onclick="openArtilleryRegimentForm('@Model.Token.Id', '@Model.ExistingBrigade.Id')">
                                        <i class="fas fa-plus"></i> Add New Artillery Regiment
                                    </button>
                                </div>
                            </div>
                            <div id="logistics-data" class="data-tab-content">
                                <div class="compact-tab-header">
                                    <h6><i class="fas fa-truck text-success"></i> Logistics Units</h6>
                                    <span class="unit-count-badge">
                                        @if (Model.ExistingLogistics != null)
                                        {
                                            <span class="badge badge-success">1 UNIT</span>
                                        }
                                    </span>
                                </div>
                                
                                <div id="logisticsUnitsList" class="units-list">
                                    @if (Model.ExistingLogistics != null)
                                    {
                                        <div class="unit-item">
                                            <div class="unit-info">
                                                <div class="unit-name1">@Model.ExistingLogistics.Name</div>
                                                <div class="unit-details">Code: @Model.ExistingLogistics.UnitCode | Strength: @Model.ExistingLogistics.Strength | Supply Trucks: @Model.ExistingLogistics.SupplyTrucks</div>
                                            </div>
                                            <div class="unit-actions">
                                                <button class="btn btn-sm btn-outline-secondary" onclick="editBrigadeUnit('logistics', '@Model.Token.Id', '@Model.ExistingBrigade.Id', '@Model.ExistingLogistics.Id')">
                                                    <i class="fas fa-edit"></i> Edit
                                                </button>
                                                <button class="btn btn-sm btn-outline-danger" onclick="deleteBrigadeUnit('logistics', '@Model.ExistingLogistics.Id', '@Model.Token.Id', '@Model.ExistingBrigade.Id')">
                                                    <i class="fas fa-trash"></i> Delete
                                                </button>
                                            </div>
                                        </div>
                                    }
                                    else
                                    {
                                        <div class="no-units-message">No logistics units found. Click "Add New" to create one.</div>
                                    }
                                </div>
                                
                                <div class="add-unit-section">
                                    <button type="button" class="btn btn-primary" onclick="openLogisticsUnitForm('@Model.Token.Id', '@Model.ExistingBrigade.Id')">
                                        <i class="fas fa-plus"></i> Add New Logistics Unit
                                    </button>
                                </div>
                            </div>
                            <div id="engineering-data" class="data-tab-content">
                                <div class="compact-tab-header">
                                    <h6><i class="fas fa-hard-hat text-secondary"></i> Combat Engineering Companies</h6>
                                    <span class="unit-count-badge">
                                        @if (Model.ExistingEngineering != null)
                                        {
                                            <span class="badge badge-secondary">1 UNIT</span>
                                        }
                                    </span>
                                </div>
                                
                                <div id="engineeringCompaniesList" class="units-list">
                                    @if (Model.ExistingEngineering != null)
                                    {
                                        <div class="unit-item">
                                            <div class="unit-info">
                                                <div class="unit-name1">@Model.ExistingEngineering.Name</div>
                                                <div class="unit-details">Code: @Model.ExistingEngineering.UnitCode | Strength: @Model.ExistingEngineering.Strength | Vehicles: @Model.ExistingEngineering.BridgeLayingVehicles</div>
                                            </div>
                                            <div class="unit-actions">
                                                <button class="btn btn-sm btn-outline-secondary" onclick="editBrigadeUnit('engineering', '@Model.Token.Id', '@Model.ExistingBrigade.Id', '@Model.ExistingEngineering.Id')">
                                                    <i class="fas fa-edit"></i> Edit
                                                </button>
                                                <button class="btn btn-sm btn-outline-danger" onclick="deleteBrigadeUnit('engineering', '@Model.ExistingEngineering.Id', '@Model.Token.Id', '@Model.ExistingBrigade.Id')">
                                                    <i class="fas fa-trash"></i> Delete
                                                </button>
                                            </div>
                                        </div>
                                    }
                                    else
                                    {
                                        <div class="no-units-message">No combat engineering companies found. Click "Add New" to create one.</div>
                                    }
                                </div>
                                
                                <div class="add-unit-section">
                                    <button type="button" class="btn btn-primary" onclick="openCombatEngineeringCompanyForm('@Model.Token.Id', '@Model.ExistingBrigade.Id')">
                                        <i class="fas fa-plus"></i> Add New Combat Engineering Company
                                    </button>
                                </div>
                            </div>
                            <div id="recon-data" class="data-tab-content">
                                <div class="compact-tab-header">
                                    <h6><i class="fas fa-binoculars text-info"></i> Reconnaissance</h6>
                                    <span class="unit-count-badge">
                                        @if (Model.ExistingRecon != null && Model.ExistingRecon.Any())
                                        {
                                            <span class="badge badge-info">@Model.ExistingRecon.Count() UNITS</span>
                                        }
                                    </span>
                                </div>
                                
                                <div id="reconnaissanceList" class="units-list">
                                    @if (Model.ExistingRecon != null && Model.ExistingRecon.Any())
                                    {
                                        @foreach (var recon in Model.ExistingRecon)
                                        {
                                            var reconData = (dynamic)recon;
                                            <div class="unit-item">
                                                <div class="unit-info">
                                                    <div class="unit-name1">@reconData.ReconType</div>
                                                    <div class="unit-details">Location: @reconData.Location | Confidence: @reconData.Confidence</div>
                                                </div>
                                                <div class="unit-actions">
                                                    <button class="btn btn-sm btn-outline-secondary" onclick="editBrigadeUnit('recon', '@Model.Token.Id', '@Model.ExistingBrigade.Id', '@reconData.Id')">
                                                        <i class="fas fa-edit"></i> Edit
                                                    </button>
                                                    <button class="btn btn-sm btn-outline-danger" onclick="deleteBrigadeUnit('recon', '@reconData.Id', '@Model.Token.Id', '@Model.ExistingBrigade.Id')">
                                                        <i class="fas fa-trash"></i> Delete
                                                    </button>
                                                </div>
                                            </div>
                                        }
                                    }
                                    else
                                    {
                                        <div class="no-units-message">No reconnaissance found. Click "Add New" to create one.</div>
                                    }
                                </div>
                                
                                <div class="add-unit-section">
                                    <button type="button" class="btn btn-primary" onclick="openReconnaissanceForm('@Model.Token.Id', '@Model.ExistingBrigade.Id')">
                                        <i class="fas fa-plus"></i> Add New Reconnaissance
                                    </button>
                                </div>
                            </div>
                        </div>
                    }
                    else if (Model.AvailableBrigades.Any())
                    {
                        <!-- Link to Existing Brigade -->
                        <div class="alert alert-warning">
                            <i class="fas fa-link"></i> Link this token to an existing brigade or create new brigade data.
                        </div>
                        
                        <div class="form-group">
                            <label>Select Existing Brigade:</label>
                            <select class="form-control" id="brigadeSelector">
                                <option value="">-- Select a Brigade --</option>
                                @foreach (var brigade in Model.AvailableBrigades)
                                {
                                    <option value="@brigade.Id">Brigade @brigade.BrigadeCode</option>
                                }
                            </select>
                        </div>
                        
                        <div class="text-center mb-3">
                            <button type="button" class="btn btn-primary" onclick="linkSelectedBrigade('@Model.Token.Id')">
                                <i class="fas fa-link"></i> Link Brigade to Token
                            </button>
                        </div>
                        
                        <hr>
                        <p class="text-center text-muted">OR</p>
                        
                        <div class="text-center">
                            <button type="button" class="btn btn-success" onclick="createNewBrigadeData('@Model.Token.Id')">
                                <i class="fas fa-plus"></i> Create New Brigade Data
                            </button>
                        </div>
                    }
                    else
                    {
                        <!-- No Brigade - Direct Unit Entry Options -->
                        <div class="alert alert-info">
                            <i class="fas fa-info-circle"></i> 
                            <strong>Flexible Unit Entry:</strong> You can add units directly to this token or create a brigade first.
                        </div>
                        
                        <!-- Direct Unit Entry Options -->
                        <div class="direct-unit-entry-section">
                            <h6><i class="fas fa-users"></i> Add Units Directly to Token</h6>
                            <p class="text-muted">Add military units directly to this token without requiring a brigade.</p>
                            
                            <!-- Show existing units without brigade -->
                            @if (Model.ExistingInfantry.Any() || Model.ExistingArmoured.Any() || Model.ExistingArtillery.Any() || Model.ExistingLogistics != null || Model.ExistingEngineering != null)
                            {
                                <div class="existing-direct-units-summary">
                                    <h6><i class="fas fa-list"></i> Existing Units</h6>
                                    
                                    @if (Model.ExistingInfantry.Any())
                                    {
                                        <div class="unit-summary-item">
                                            <i class="fas fa-user-shield"></i>
                                            <strong>Infantry Battalions:</strong> @Model.ExistingInfantry.Count
                                            <button class="btn btn-sm btn-outline-primary" onclick="openDirectUnitForm('infantry', '@Model.Token.Id')">
                                                <i class="fas fa-eye"></i> View/Manage
                                            </button>
                                        </div>
                                    }
                                    
                                    @if (Model.ExistingArmoured.Any())
                                    {
                                        <div class="unit-summary-item">
                                            <i class="fas fa-shield-alt"></i>
                                            <strong>Armoured Regiments:</strong> @Model.ExistingArmoured.Count
                                            <button class="btn btn-sm btn-outline-primary" onclick="openDirectUnitForm('armoured', '@Model.Token.Id')">
                                                <i class="fas fa-eye"></i> View/Manage
                                            </button>
                                        </div>
                                    }
                                    
                                    @if (Model.ExistingArtillery.Any())
                                    {
                                        <div class="unit-summary-item">
                                            <i class="fas fa-crosshairs"></i>
                                            <strong>Artillery Regiments:</strong> @Model.ExistingArtillery.Count
                                            <button class="btn btn-sm btn-outline-primary" onclick="openDirectUnitForm('artillery', '@Model.Token.Id')">
                                                <i class="fas fa-eye"></i> View/Manage
                                            </button>
                                        </div>
                                    }
                                    
                                    @if (Model.ExistingLogistics != null)
                                    {
                                        <div class="unit-summary-item">
                                            <i class="fas fa-truck"></i>
                                            <strong>Logistics Unit:</strong> @Model.ExistingLogistics.Name
                                            <button class="btn btn-sm btn-outline-primary" onclick="openDirectUnitForm('logistics', '@Model.Token.Id')">
                                                <i class="fas fa-eye"></i> View/Manage
                                            </button>
                                        </div>
                                    }
                                    
                                    @if (Model.ExistingEngineering != null)
                                    {
                                        <div class="unit-summary-item">
                                            <i class="fas fa-hard-hat"></i>
                                            <strong>Engineering Company:</strong> @Model.ExistingEngineering.Name
                                            <button class="btn btn-sm btn-outline-primary" onclick="openDirectUnitForm('engineering', '@Model.Token.Id')">
                                                <i class="fas fa-eye"></i> View/Manage
                                            </button>
                                        </div>
                                    }
                                </div>
                                
                                <hr style="margin: 20px 0; border-top: 2px dashed #dee2e6;" />
                            }
                            
                            <h6><i class="fas fa-plus-circle"></i> Add More Units</h6>
                            <div class="direct-unit-buttons">
                                <button type="button" class="btn btn-outline-primary btn-sm" onclick="openDirectUnitForm('infantry', '@Model.Token.Id')">
                                    <i class="fas fa-user-shield"></i> Infantry Battalion
                                </button>
                                <button type="button" class="btn btn-outline-primary btn-sm" onclick="openDirectUnitForm('armoured', '@Model.Token.Id')">
                                    <i class="fas fa-shield-alt"></i> Armoured Regiment
                                </button>
                                <button type="button" class="btn btn-outline-primary btn-sm" onclick="openDirectUnitForm('artillery', '@Model.Token.Id')">
                                    <i class="fas fa-crosshairs"></i> Artillery Regiment
                                </button>
                                <button type="button" class="btn btn-outline-primary btn-sm" onclick="openDirectUnitForm('logistics', '@Model.Token.Id')">
                                    <i class="fas fa-truck"></i> Logistics Unit
                                </button>
                                <button type="button" class="btn btn-outline-primary btn-sm" onclick="openDirectUnitForm('engineering', '@Model.Token.Id')">
                                    <i class="fas fa-hard-hat"></i> Engineering
                                </button>
                            </div>
                        </div>
                        
                        <hr>
                        <p class="text-center text-muted">OR</p>
                        
                        <!-- Brigade Creation Option -->
                        <div class="brigade-creation-section">
                            <h6><i class="fas fa-flag"></i> Create Brigade First</h6>
                            <p class="text-muted">Create a brigade to organize units under a command structure.</p>
                            
                            <div class="text-center">
                                <button type="button" class="btn btn-success" onclick="createNewBrigadeData('@Model.Token.Id')">
                                    <i class="fas fa-plus"></i> Create New Brigade Data
                                </button>
                            </div>
                        </div>
                    }
                </div>
            </div>
        </div>
        <div class="modal-footer">
            <button type="button" class="btn btn-secondary" data-dismiss="modal">
                <i class="fas fa-times"></i> Close
            </button>
        </div>
    </div>
    </div>
</div>

<style>
/* Professional Brigade Section - Light Theme */
.brigade-section-professional {
    background: #ffffff;
    border: 1px solid #e1e5ea;
    border-radius: 8px;
    padding: 0;
    margin-bottom: 20px;
    overflow: hidden;
}

.brigade-info-row {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 15px 20px;
}

.brigade-basic-info {
    display: flex;
    align-items: center;
    gap: 12px;
}

.brigade-label {
    color: #6c757d;
    font-size: 16px;
    font-weight: 500;
}

.brigade-code-display {
    color: #344767;
    font-size: 20px;
    font-weight: 600;
}

.btn-outline-warning {
    border-color: #ffa726;
    color: #ffa726;
    background: transparent;
    padding: 8px 16px;
    font-size: 16px;
}

.btn-outline-warning:hover {
    background: #ffa726;
    color: white;
    border-color: #ffa726;
}

/* Compact Data Entry Tabs */
.data-entry-tabs.compact-style {
    background: transparent;
    padding: 0;
    margin-bottom: 10px;
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
}

.data-entry-tabs.compact-style .data-tab {
    flex: 1;
    min-width: 120px;
    padding: 12px 16px;
    font-size: 16px;
    border-radius: 6px;
}

/* Compact Tab Headers - Professional Light Theme */
.compact-tab-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 10px 15px;
    background: #f8f9fa;
    border-left: 3px solid #ffa726;
    border-radius: 4px;
    margin-bottom: 12px;
}

.compact-tab-header h6 {
    margin: 0;
    font-size: 18px;
    font-weight: 600;
    color: #ffa726;
}

.compact-tab-header i {
    margin-right: 8px;
    font-size: 18px;
}

.unit-count-badge .badge {
    font-size: 14px;
    padding: 6px 10px;
    letter-spacing: 0.5px;
}

.badge-primary {
    background: #9c27b0;
    color: white;
}

.badge-warning {
    background: #ff9800;
    color: white;
}

.badge-danger {
    background: #f44336;
    color: white;
}

.badge-success {
    background: #4caf50;
    color: white;
}

.badge-secondary {
    background: #757575;
    color: white;
}

.badge-info {
    background: #00bcd4;
    color: white;
}

/* Light Theme Data Entry Form */
.data-entry-container {
    background: #fafbfc;
    border-radius: 8px;
    padding: 20px;
}

/* Professional Section Title Bar - Orange Accent */
.section-title-bar {
    background: #f8f9fa;
    border-bottom: 2px solid #ffa726;
    padding: 12px 18px;
    margin-bottom: 15px;
    font-size: 18px;
    font-weight: 600;
    letter-spacing: 0.5px;
    color: #ffa726;
    border-radius: 4px 4px 0 0;
}

.section-title-bar i {
    margin-right: 10px;
    font-size: 18px;
}

/* Token Base Section - Light Professional Theme */
.token-base-section {
    background: #ffffff;
    border: 1px solid #e1e5ea;
    border-radius: 8px;
    padding: 0;
    margin-bottom: 20px;
    overflow: hidden;
}

/* Horizontal layout for token info and status note */
.token-content-row {
    display: flex;
    align-items: center;
    gap: 20px;
    justify-content: space-between;
    padding: 15px 20px;
}

.token-display {
    display: flex;
    align-items: center;
    gap: 15px;
    flex: 0 0 auto;
}

/* Token status note - Professional Light Theme */
.token-status-note {
    flex: 1;
    background: #fff8e1;
    border: 1px solid #ffa726;
    border-left: 4px solid #ffa726;
    border-radius: 6px;
    padding: 15px 18px;
    display: flex;
    align-items: center;
    gap: 12px;
    color: #344767;
    font-size: 16px;
    min-width: 0;
}

.token-status-note i {
    color: #ffa726;
    font-size: 20px;
    flex-shrink: 0;
}

.token-status-note span {
    line-height: 1.4;
    color: #344767;
}

.token-status-note strong {
    color: #ffa726;
}

.token-preview-image {
    width: 60px;
    height: 60px;
    border-radius: 8px;
    object-fit: cover;
    border: 2px solid #ffa726;
}

.token-preview-placeholder {
    width: 70px;
    height: 70px;
    border-radius: 8px;
    background: #ffa726;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    font-size: 28px;
    border: 2px solid #ffa726;
    font-weight: bold;
}

.token-details h4 {
    margin: 0;
    color: #344767;
    font-size: 22px;
    font-weight: 600;
}

.token-group-name {
    color: #6c757d;
    font-size: 16px;
    margin: 4px 0 0 0;
}

.token-status {
    margin-top: 8px;
    display: flex;
    gap: 8px;
}

/* Professional Solid Badges */
.badge-success-solid {
    background: #4caf50;
    color: white;
    padding: 6px 12px;
    border-radius: 4px;
    font-size: 14px;
    font-weight: 600;
    letter-spacing: 0.5px;
}

.badge-blue-solid {
    background: #2196f3;
    color: white;
    padding: 6px 12px;
    border-radius: 4px;
    font-size: 14px;
    font-weight: 600;
    letter-spacing: 0.5px;
}

.badge {
    padding: 6px 10px;
    border-radius: 4px;
    font-size: 14px;
    font-weight: 600;
}

.badge-primary {
    background: #007bff;
    color: white;
}

.badge-info {
    background: #17a2b8;
    color: white;
}

.direct-unit-entry-section {
    background: #f8f9fa;
    border: 1px solid #dee2e6;
    border-radius: 8px;
    padding: 20px;
    margin-bottom: 20px;
}

.direct-unit-entry-section h6 {
    color: #495057;
    margin-bottom: 10px;
    font-weight: 600;
    font-size: 18px;
}

.direct-unit-buttons {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
    margin-top: 15px;
}

.direct-unit-buttons .btn {
    flex: 1;
    min-width: 140px;
    text-align: center;
}

.existing-direct-units-summary {
    background: #f8f9fa;
    border: 1px solid #dee2e6;
    border-radius: 8px;
    padding: 15px;
    margin-bottom: 20px;
}

.existing-direct-units-summary h6 {
    color: #2c3e50;
    margin-bottom: 15px;
    font-weight: 600;
    font-size: 18px;
}

.unit-summary-item {
    display: flex;
    align-items: center;
    gap: 12px;
    padding: 12px;
    background: white;
    border: 1px solid #dee2e6;
    border-radius: 6px;
    margin-bottom: 8px;
    transition: all 0.3s ease;
}

.unit-summary-item:hover {
    background: #e9ecef;
    border-color: #007bff;
    transform: translateX(3px);
}

.unit-summary-item i {
    color: #007bff;
    font-size: 20px;
    width: 28px;
    text-align: center;
}

.unit-summary-item strong {
    flex: 1;
    color: #2c3e50;
    font-size: 16px;
}

.unit-summary-item .btn {
    margin-left: auto;
}

.brigade-creation-section {
    background: #e8f5e8;
    border: 1px solid #28a745;
    border-radius: 8px;
    padding: 20px;
    margin-top: 20px;
}

.brigade-creation-section h6 {
    color: #28a745;
    margin-bottom: 10px;
    font-weight: 600;
    font-size: 18px;
}

.brigade-header-section {
    margin-bottom: 20px;
    padding: 15px;
    background: #ffffff;
    border-radius: 8px;
    border-left: 4px solid #00d4ff;
}

.brigade-name-display {
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.brigade-name-display h5 {
    color: #344767;
    margin: 0;
    font-weight: 600;
}

.data-entry-tabs {
    display: flex;
    gap: 3px;
    margin: 20px 0 10px 0;
    background: #f8f9fa;
    border-radius: 8px;
    padding: 5px;
}

.data-tab {
    flex: 1;
    padding: 14px 10px;
    border: 1px solid #e1e5ea;
    background: #ffffff;
    color: #344767;
    cursor: pointer;
    border-radius: 6px;
    text-align: center;
    font-size: 15px;
    transition: all 0.3s ease;
}

.data-tab:hover {
    background: #f8f9fa;
    color: #344767;
    border-color: #00d4ff;
}

.data-tab.active {
    background: #00d4ff;
    color: #000;
    border-color: #00d4ff;
    font-weight: 600;
}

.data-tab i {
    display: block;
    margin-bottom: 4px;
    font-size: 18px;
}

.data-tab-content {
    display: none;
    padding: 20px;
    border: 1px solid #e1e5ea;
    border-radius: 8px;
    background: #ffffff;
    margin-top: 10px;
}

.data-tab-content.active {
    display: block;
}

.tab-content-header {
    margin-bottom: 15px;
    padding-bottom: 10px;
    border-bottom: 1px solid #e1e5ea;
}

.tab-content-header h6 {
    color: #00d4ff;
    margin: 0 0 5px 0;
    font-weight: 600;
    font-size: 18px;
}

.tab-content-header .text-muted {
    color: #6c757d !important;
    font-size: 16px;
}

.btn {
    padding: 12px 24px;
    border-radius: 6px;
    font-weight: 600;
    font-size: 16px;
    transition: all 0.3s ease;
}

.btn-primary {
    background: #00d4ff;
    border-color: #00d4ff;
    color: #000;
}

.btn-primary:hover {
    background: #00b8e6;
    border-color: #00b8e6;
    color: #000;
}

.btn-outline-primary {
    background: transparent;
    border-color: #00d4ff;
    color: #00d4ff;
}

.btn-outline-primary:hover {
    background: #00d4ff;
    border-color: #00d4ff;
    color: #000;
}

.alert {
    padding: 12px 15px;
    border-radius: 6px;
    margin-bottom: 15px;
    border-left: 4px solid;
}

.alert-info {
    background: #e3f2fd;
    border-left-color: #00d4ff;
    color: #344767;
}

.alert-warning {
    background: #fff3cd;
    border-left-color: #ffaa00;
    color: #344767;
}

.alert-success {
    background: #d4edda;
    border-left-color: #00aa44;
    color: #344767;
}

.form-control {
    background: #f8f9fa;
    border: 1px solid #e1e5ea;
    color: #344767;
    border-radius: 6px;
    padding: 12px 16px;
    font-size: 16px;
}

.form-control:focus {
    background: #ffffff;
    border-color: #00d4ff;
    color: #344767;
    box-shadow: 0 0 0 0.2rem rgba(0, 212, 255, 0.25);
}

.text-center {
    text-align: center;
}

.text-muted {
    color: #6c757d !important;
}

.mb-3 {
    margin-bottom: 1rem;
}

hr {
    border-color: #e1e5ea;
    margin: 20px 0;
}

/* Units List Styling */
.units-list {
    margin-bottom: 20px;
    max-height: 300px;
    overflow-y: auto;
}

.loading-units {
    text-align: center;
    padding: 20px;
    color: #6c757d;
    font-style: italic;
}

.loading-units i {
    margin-right: 8px;
    color: #00d4ff;
}

.unit-item {
    background: #ffffff;
    border: 1px solid #e1e5ea;
    border-radius: 6px;
    padding: 12px;
    margin-bottom: 8px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    transition: all 0.3s ease;
}

.unit-item:hover {
    background: #f8f9fa;
    border-color: #00d4ff;
}

.unit-info {
    flex: 1;
    margin-right: 12px;
    overflow: hidden;
}

.unit-name1 {
    color: #344767;
    font-weight: 600;
    font-size: 16px;
    margin-bottom: 4px;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
}

.unit-details {
    color: #6c757d;
    font-size: 14px;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
}

.unit-actions {
    display: flex;
    gap: 8px;
    flex-shrink: 0;
}

.btn-sm {
    padding: 8px 16px;
    font-size: 14px;
    border-radius: 4px;
}

.btn-outline-secondary {
    background: transparent;
    border-color: #6c757d;
    color: #6c757d;
}

.btn-outline-secondary:hover {
    background: #6c757d;
    border-color: #6c757d;
    color: #fff;
}

.btn-outline-danger {
    background: transparent;
    border-color: #ff4444;
    color: #ff4444;
}

.btn-outline-danger:hover {
    background: #ff4444;
    border-color: #ff4444;
    color: #fff;
}

.add-unit-section {
    text-align: center;
    padding: 15px;
    border-top: 1px solid #e1e5ea;
    margin-top: 10px;
}

.no-units-message {
    text-align: center;
    padding: 30px;
    color: #6c757d;
    font-style: italic;
    font-size: 16px;
    background: #f8f9fa;
    border-radius: 6px;
    border: 1px solid #e1e5ea;
}

/* Scrollbar styling for units list */
.units-list::-webkit-scrollbar {
    width: 6px;
}

.units-list::-webkit-scrollbar-track {
    background: #f8f9fa;
}

.units-list::-webkit-scrollbar-thumb {
    background: #cbd5e0;
    border-radius: 3px;
}

.units-list::-webkit-scrollbar-thumb:hover {
    background: #a0aec0;
}
</style>

<script>
// Close token data entry modal - Bootstrap Modal API
function closeTokenDataEntryModal() {
    console.log('🔒 Closing token data entry modal');
    $('#tokenDataEntryModal').modal('hide');
}

// Show different data tabs
function showDataTab(tabName) {
    // Hide all tab contents
    document.querySelectorAll('.data-tab-content').forEach(tab => {
        tab.classList.remove('active');
    });
    
    // Remove active class from all tabs
    document.querySelectorAll('.data-tab').forEach(tab => {
        tab.classList.remove('active');
    });
    
    // Show selected tab content
    document.getElementById(tabName + '-data').classList.add('active');
    
    // Activate clicked tab
    event.target.classList.add('active');
}

// Link selected brigade to token
function linkSelectedBrigade(tokenId) {
    const brigadeId = document.getElementById('brigadeSelector').value;
    if (!brigadeId) {
        alert('Please select a brigade first');
        return;
    }
    
    console.log('🔗 Linking brigade', brigadeId, 'to token', tokenId);
    
    // Show loading
    showLoader();
    
    // Make AJAX call to link brigade to token
    $.ajax({
        url: '/DataManagement/LinkBrigadeToToken',
        type: 'POST',
        contentType: 'application/json',
        data: JSON.stringify({
            TokenId: tokenId,
            BrigadeId: brigadeId
        }),
        success: function(response) {
            console.log('✅ Brigade linked successfully:', response);
            
            if (response.success) {
                console.log('✅ Brigade linked, no page refresh needed');
                closeTokenDataEntryModal();
                hideLoader();
                // Modal will be refreshed when reopened
            } else {
                hideLoader();
                alert('Error linking brigade: ' + response.message);
            }
        },
        error: function(xhr, status, error) {
            hideLoader();
            console.error('❌ Error linking brigade:', error);
            console.error('   Status Code:', xhr.status);
            console.error('   Response Text:', xhr.responseText);
            
            let errorMessage = 'Failed to link brigade to token';
            if (xhr.status === 404) {
                errorMessage = 'Brigade linking endpoint not found (404)';
            } else if (xhr.status === 500) {
                errorMessage = 'Server error linking brigade (500)';
            }
            
            alert(errorMessage + '. Please try again.');
        }
    });
}

// Create new brigade data
function createNewBrigadeData(tokenId) {
    console.log('🆕 Creating new brigade data for token', tokenId);
    
    // Close current modal
    closeTokenDataEntryModal();
    
    // Show loading
    showLoader();
    
    // Load the new brigade creation form via AJAX
    $.ajax({
        url: '/DataManagement/NewBrigadeDataEntryForm',
        type: 'GET',
        data: { tokenId: tokenId },
        success: function(data) {
            console.log('✅ New brigade form loaded for token:', tokenId);
            
            // Remove any existing new brigade modal
            $('#newBrigadeDataEntryModal').remove();
            
            // Add the new modal to the body
            $('body').append(data);
            
            // Show the modal using Bootstrap modal API
            setTimeout(() => {
                $('#newBrigadeDataEntryModal').modal('show');
                hideLoader();
            }, 100);
            
            console.log('✅ New brigade form displayed successfully');
        },
        error: function(xhr, status, error) {
            hideLoader();
            console.error('❌ Error loading new brigade form:', error);
            console.error('   Status Code:', xhr.status);
            console.error('   Response Text:', xhr.responseText);
            
            let errorMessage = 'Failed to load new brigade form';
            if (xhr.status === 404) {
                errorMessage = 'New brigade form not found (404)';
            } else if (xhr.status === 500) {
                errorMessage = 'Server error loading new brigade form (500)';
            }
            
            alert(errorMessage + '. Please try again.');
        }
    });
}

// Edit brigade code
function editBrigadeCode() {
    const currentCode = document.querySelector('.brigade-name-display h5').textContent.replace('Brigade ', '').replace(' Information', '');
    const newCode = prompt('Enter new brigade code:', currentCode);
    
    if (newCode && newCode.trim() !== '' && newCode !== currentCode) {
        console.log('✏️ Updating brigade code from', currentCode, 'to', newCode);
        
        // Update the display
        document.querySelector('.brigade-name-display h5').textContent = 'Brigade ' + newCode + ' Information';
        
        // Update all references in tab content
        document.querySelectorAll('.tab-content-header .text-muted').forEach(element => {
            element.textContent = element.textContent.replace('Brigade ' + currentCode, 'Brigade ' + newCode);
        });
        
        // Here you would typically make an AJAX call to save the new code
        // For now, just show a success message
        alert('Brigade code updated successfully!');
    }
}

// Load existing units when modal opens
// Data is now loaded in the controller, no need for AJAX loading

// Data is now loaded in the controller, no need for AJAX loading

// Data is now loaded in the controller, no need for AJAX loading

// Data is now loaded in the controller, no need for AJAX loading

// Data is now loaded in the controller, no need for AJAX loading

// Data is now loaded in the controller, no need for AJAX loading

// Open individual unit forms (removed duplicates - using new functions below)

// Load single unit form
function loadSingleUnitForm(unitType, tokenId, brigadeId) {
    showLoader();
    
    $.ajax({
        url: '/DataManagement/SingleUnitForm',
        type: 'GET',
        data: { unitType: unitType, tokenId: tokenId, brigadeId: brigadeId },
        success: function(data) {
            console.log('✅ Single unit form loaded');
            
            // Remove any existing single unit modal
            $('#singleUnitModal').remove();
            
            // Add the new modal to the body
            $('body').append(data);
            
            // Show the modal using Bootstrap modal API
            setTimeout(() => {
                $('#singleUnitModal').modal('show');
                hideLoader();
            }, 100);
            
            console.log('✅ Single unit form displayed successfully');
        },
        error: function(xhr, status, error) {
            hideLoader();
            console.error('❌ Error loading single unit form:', error);
        }
    });
}

// Refresh units list after save - No page reload
function refreshUnitsList(tokenId, brigadeId) {
    console.log('🔄 Refreshing units list (no page reload)');
    // Modal will be reloaded when user reopens it
}

// Open direct unit form (without brigade)
function openDirectUnitForm(unitType, tokenId) {
    console.log('🆕 Opening direct unit form for', unitType, 'token', tokenId);
    
    showLoader();
    
    // Load the direct unit form via AJAX (without brigade)
    $.ajax({
        url: '/DataManagement/DirectUnitForm',
        type: 'GET',
        data: { unitType: unitType, tokenId: tokenId, _cacheBust: new Date().getTime() },
        success: function(data) {
            console.log('✅ Direct unit form loaded');
            
            // Remove any existing direct unit modal
            $('#directUnitModal').remove();
            
            // Add the new modal to the body
            $('body').append(data);
            
            // Show the modal using Bootstrap modal API
            setTimeout(() => {
                $('#directUnitModal').modal('show');
                hideLoader();
            }, 100);
            
            console.log('✅ Direct unit form displayed successfully');
        },
        error: function(xhr, status, error) {
            hideLoader();
            console.error('❌ Error loading direct unit form:', error);
            alert('Failed to load unit form. Please try again.');
        }
    });
}

// Global brigade unit edit function (with all parameters)
function editBrigadeUnit(unitType, tokenId, brigadeId, unitId) {
    console.log('✏️ Editing brigade unit:', { unitType, tokenId, brigadeId, unitId });
    
    showLoader();
    
    const params = {
        unitType: unitType,
        tokenId: tokenId,
        brigadeId: brigadeId,
        unitId: unitId
    };
    
    $.get('/DataManagement/BrigadeUnitCreationForm', params, function(data) {
        $('#brigadeUnitCreationModal').remove();
        $('body').append(data);
        $('#brigadeUnitCreationModal').modal('show');
        hideLoader();
    }).fail(function(xhr, status, error) {
        hideLoader();
        console.error('❌ Error loading unit form:', error);
        alert('Failed to load unit form: ' + error);
    });
}

// Global brigade unit delete function (with all parameters)
function deleteBrigadeUnit(unitType, unitId, tokenId, brigadeId) {
    const unitTypeNames = {
        'infantry': 'Infantry Battalion',
        'armoured': 'Armoured Regiment',
        'artillery': 'Artillery Regiment',
        'logistics': 'Logistics Unit',
        'engineering': 'Engineering Company',
        'recon': 'Recon Unit'
    };
    
    const unitName = unitTypeNames[unitType] || 'Unit';
    
    if (!confirm(`Are you sure you want to delete this ${unitName}?`)) return;
    
    const endpoints = {
        'infantry': '/DataManagement/DeleteTokenInfantryBattalion',
        'armoured': '/DataManagement/DeleteTokenArmouredRegiment',
        'artillery': '/DataManagement/DeleteTokenArtilleryRegiment',
        'logistics': '/DataManagement/DeleteTokenLogisticsUnit',
        'engineering': '/DataManagement/DeleteTokenCombatEngineeringCompany',
        'recon': '/DataManagement/DeleteTokenRecon'
    };
    
    const endpoint = endpoints[unitType];
    
    showLoader();
    
    $.ajax({
        url: `${endpoint}?id=${unitId}`,
        type: 'DELETE',
        success: function(response) {
            console.log(`✅ ${unitName} deleted successfully, response:`, response);
            
            if (typeof toastr !== 'undefined') {
                toastr.success(`${unitName} deleted successfully`, 'Success');
            }
            
            // Reload the token data entry modal to show updated units
            console.log('🔄 Reloading TokenDataEntryForm with tokenId:', tokenId);
            $.get('/DataManagement/TokenDataEntryForm', { 
                tokenId: tokenId, 
                _cacheBust: new Date().getTime() 
            }, function(data) {
                console.log('✅ Received TokenDataEntryForm data, length:', data.length);
                $('#tokenDataEntryModal .modal-content').html($(data).find('.modal-content').html());
                hideLoader();
                console.log('✅ Token data entry modal reloaded');
            }).fail(function() {
                hideLoader();
                alert('Deleted but failed to refresh. Please close and reopen the modal.');
            });
        },
        error: function(xhr, status, error) {
            hideLoader();
            alert(`Failed to delete ${unitName}: ` + error);
        }
    });
}

// Brigade unit form opening functions (missing from TokenDataEntryForm)
function openInfantryBattalionForm(tokenId, brigadeId, unitId = null) {
    openBrigadeUnitForm('infantry', tokenId, brigadeId, unitId);
}

function openArmouredRegimentForm(tokenId, brigadeId, unitId = null) {
    openBrigadeUnitForm('armoured', tokenId, brigadeId, unitId);
}

function openArtilleryRegimentForm(tokenId, brigadeId, unitId = null) {
    openBrigadeUnitForm('artillery', tokenId, brigadeId, unitId);
}

function openLogisticsUnitForm(tokenId, brigadeId, unitId = null) {
    openBrigadeUnitForm('logistics', tokenId, brigadeId, unitId);
}

function openCombatEngineeringCompanyForm(tokenId, brigadeId, unitId = null) {
    openBrigadeUnitForm('engineering', tokenId, brigadeId, unitId);
}

function openReconnaissanceForm(tokenId, brigadeId, unitId = null) {
    openBrigadeUnitForm('recon', tokenId, brigadeId, unitId);
}

// Generic brigade unit form opener with loader
function openBrigadeUnitForm(unitType, tokenId, brigadeId, unitId = null) {
    console.log('🚀 Loading brigade unit form:', { unitType, tokenId, brigadeId, unitId });
    
    const params = {
        unitType: unitType,
        tokenId: tokenId,
        brigadeId: brigadeId
    };
    
    if (unitId) {
        params.unitId = unitId;
    }
    
    showLoader();
    
    $.get('/DataManagement/BrigadeUnitCreationForm', params, function(data) {
        // Remove any existing modal
        $('#brigadeUnitCreationModal').remove();
        
        // Append the new modal to body
        $('body').append(data);
        
        // Show the modal
        $('#brigadeUnitCreationModal').modal('show');
        
        hideLoader();
        console.log('✅ Brigade unit form loaded successfully');
    }).fail(function(xhr, status, error) {
        hideLoader();
        console.error('❌ Error loading brigade unit form:', error);
        alert('Failed to load unit form: ' + error);
    });
}

// Data is now loaded in the controller, no need for initialization
</script>
